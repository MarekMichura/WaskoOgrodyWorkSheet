# Etap 1: Base
FROM node:16-alpine AS html-release
WORKDIR /html
RUN mkdir build
COPY tsconfig.json ./
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# Etap 2: Development
FROM node:16-alpine AS html-development
WORKDIR /html
EXPOSE 3000
CMD ["npm", "start"]
