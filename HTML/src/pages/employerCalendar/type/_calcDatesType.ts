export interface calcDatesType {
  prev: {
    year: number
    month: number
  }
  curr: {
    breakDay1: number
    breakDay2: number
    prevStart: number
  }
  next: {
    year: number
    month: number
  }
}
